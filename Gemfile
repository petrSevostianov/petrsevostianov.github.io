# frozen_string_literal: true

source "https://rubygems.org"

# Use the GitHub Pages gem to match GitHub Pages environment
gem "github-pages", group: :jekyll_plugins

group :development do
  # For local server and live reload
  gem "webrick", "~> 1.8"
end
